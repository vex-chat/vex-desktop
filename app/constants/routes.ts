interface IRoutes {
    HOME: string;
    MESSAGING: string;
    REGISTER: string;
    SETTINGS: string;
    LAUNCH: string;
    SERVERS: string;
    CREATE: string;
    LOGIN: string;
}

export const routes: IRoutes = {
    HOME: "/",
    MESSAGING: "/messaging",
    REGISTER: "/register",
    SETTINGS: "/settings",
    LAUNCH: "/launch",
    SERVERS: "/server",
    CREATE: "/create",
    LOGIN: "/login",
};
